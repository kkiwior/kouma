apiVersion: v1
kind: Service
metadata:
  name: {{ include "kouma.fullname" . }}-dashboard
  labels:
    {{- include "kouma.dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.dashboard.service.type }}
  ports:
    - port: {{ .Values.dashboard.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.dashboard.service.type "NodePort") .Values.dashboard.service.nodePort }}
      nodePort: {{ .Values.dashboard.service.nodePort }}
      {{- end }}
  selector:
    {{- include "kouma.dashboard.selectorLabels" . | nindent 4 }}
